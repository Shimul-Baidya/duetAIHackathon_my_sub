# ---- competition data (do NOT push; Kaggle loads it from /kaggle/input) ----
data/

# ---- generated locally / on Kaggle (regenerated by the notebook) ----
track2/yolo_ds/          # CSV->YOLO labels, symlinks, fold lists (rebuilt by build_dataset)
track2/runs/             # training outputs + weights
track2/out/              # local submission/cache scratch
caches/
submission*.csv
final_submission*.csv
*.pt                     # model weights

# ---- python ----
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.DS_Store
